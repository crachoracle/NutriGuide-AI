{
  "name": "nutriguide-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NutriGuide AI hackathon MVP with a React client, Express API, mock OCR, menu recommendations, and demo journal storage.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.cjs",
    "server": "npm run dev --workspace server",
    "client": "npm run dev --workspace client",
    "build": "npm run build --workspace client && node scripts/sync-client-dist.cjs",
    "demo": "npm run build && npm start",
    "start": "npm run start --workspace server"
  }
}
